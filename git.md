# this file contains git commnands

- `git checkout -b <branch-name>` # this is used to crerate a new branch
- `git status` # check you working tree
- `git add`
- `git commit -m "<your commit message>"`
- `git push`